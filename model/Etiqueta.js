/*
const banco = require('../config/database');
const Chave = require('../model/Chave');
const Etiqueta = banco.db.define("etiqueta",{
    gerar: {
        type: banco.Sequelize.BOOLEAN,
    }
});
*/





